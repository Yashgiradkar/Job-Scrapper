# Python Scripts

Reserved for site-specific scraping scripts that are easier to implement in Python (e.g. complex anti-bot bypass, API reverse-engineering).

The Node.js `ScraperRunner` can invoke these via `child_process` when needed. Not used in the initial MVP.
